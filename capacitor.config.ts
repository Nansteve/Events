import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'projet.evential',
  appName: 'Evential',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
